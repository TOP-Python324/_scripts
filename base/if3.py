number = 10
text = input(' ввод: ')

# if number != 0:
# вместо выражения выше можно использовать неявное приведение к объекту bool
if number:
    print(f'{bool(number)}')

# if text != '':
# вместо выражения выше можно использовать неявное приведение к объекту bool
if text:
    print(f'{bool(text)}')


